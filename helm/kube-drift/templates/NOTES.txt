kube-drift {{ .Chart.AppVersion }} has been installed as release "{{ .Release.Name }}".

The controller is running in the "{{ include "kube-drift.namespace" . }}" namespace.

1. Check the controller is up:

   kubectl -n {{ include "kube-drift.namespace" . }} rollout status deployment/{{ include "kube-drift.fullname" . }}-controller-manager

2. The DriftCheck CRD is installed cluster-wide. Create one to start a check:

   kubectl apply -f https://raw.githubusercontent.com/somaz94/kube-drift/{{ .Chart.AppVersion }}/config/samples/driftcheck_v1alpha1_sample.yaml

3. Inspect DriftCheck resources:

   kubectl get driftchecks -A

{{- if .Values.metrics.enabled }}

Metrics are served on port {{ .Values.metrics.port }} ({{ if .Values.metrics.secure }}HTTPS, authn/authz protected{{ else }}HTTP{{ end }}) via
Service "{{ include "kube-drift.fullname" . }}-controller-manager-metrics-service".
{{- end }}

NOTE: This is an early scaffold (v0.1.x) — the reconcile logic is a stub and does
not perform drift detection yet.
